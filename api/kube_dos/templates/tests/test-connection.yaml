apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kube_dos.fullname" . }}-test-connection"
  labels:
    {{- include "kube_dos.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
  restartPolicy: Never
